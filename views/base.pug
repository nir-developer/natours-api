doctype html
html(lang="en")
    head
        title Natours #{tour}
        link(rel='stylesheet' href='css/style.css')
        link(rel='shortcut icon' type='image/png' href='img/favicon.png')
    body 
        h1= tour.toUpperCase()
        //-h1 The Park Camper
        p= user

        //UN BUFFERED CODE
        - const x = 2

        //BUFFERED CODE
        p= x * 2
